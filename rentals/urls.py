from django.urls import path
from . import views

app_name = "rentals"

urlpatterns = [
    # Adjust these to your actual views later
    # Example:
    # path("", views.rental_list, name="rental_list"),
]
